#include "../nodes/node.hh"
#include <functional>


namespace doanimate {
	namespace shapes {
		nodes::node* line();
		nodes::node* polygon();
		nodes::node* regular_polygon();
		nodes::node* ellipse();
		nodes::node* circle();
	}
}
